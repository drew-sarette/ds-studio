<nav aria-label="Change page">
  <ol role="list" class="paginated secondary-nav-list">
    {% if page.url != pagination.href.first %}
      <li class="secondary-nav-item">
        <a class="secondary-nav-link" href="{{ pagination.href.first }}">First</a>
      </li>
    {% endif %}
    {% if pagination.href.previous %}
      <li class="secondary-nav-item">
        <a class="secondary-nav-link" href="{{ pagination.href.previous }}">Previous</a>
      </li>
    {% endif %}
    {%- for pageEntry in pagination.pages %}
      <li class="secondary-nav-item">
        <a class="secondary-nav-link" href="{{ pagination.hrefs[ loop.index0 ] }}" {% if page.url == pagination.hrefs[ loop.index0 ] %} aria-current="page" {% endif %}>{{ loop.index }}</a>
      </li>
    {%- endfor %}
    {% if pagination.href.next %}
      <li class="secondary-nav-item">
        <a class="secondary-nav-link" href="{{ pagination.href.next }}">Next</a>
      </li>
    {% endif %}
    {% if page.url != pagination.href.last %}
      <li class="secondary-nav-item">
        <a class="secondary-nav-link" href="{{ pagination.href.last }}">Last</a>
      </li>
    {% endif %}
  </ol>
</nav>