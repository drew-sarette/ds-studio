<article data-pagefind-body class="main-article">
    <header class="article-header">
        <h1 class="article-title">
            {{ title }}
        </h1>
        <p>posted on {{ date | postDate }}</p>
    </header>
    {% if vimeoId %}
        <vimeo-embed vimeoId="{{ vimeoId }}"></vimeo-embed>
    {% elseif img %}
        <figure class="article-figure">
            <img class="article-main-img" src="{{ img }}" alt="{{ imgAlt }}">
                <figcaption>
                    {{ imgAlt }}
                </figcaption>
            </figure>
        {% endif %}
        {% if pdf %}
            <a href="{{ pdf }}" class="download-link" download>Download PDF</a>
        {% endif %}
        {{ content | safe }}
    </article>