---
title: Lessons
layout: 'layouts/base.njk'
---

<section>
    <h1>Lessons</h1>
    <p>Book a free 30-minute trial lesson with me here!</p>
</section>
<section>
    <h2>Before your first lesson...</h2>
    <p>These articles contain everything you need to know before your first lesson.</p>
    <ul role="list">
        {% for doc in collections.docs %}
            <li class="snippet">
                <a href="{{ doc.url }}">
                <div class="img-wrapper">
                    <img src="{{ doc.data.img }}" alt="{{ doc.data.imgAlt }}">
                </div>
                <div>
                    <h3>
                        {{ doc.data.title }}
                    </h3>
                    <p>{{ doc.data.description }}</p>
                </div>
                </a>
            </li>
        {% endfor %}
    </ul>
</section>